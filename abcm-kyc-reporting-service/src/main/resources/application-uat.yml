spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/abcm_kyc_ms_db?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: Abcmapp@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, info
  endpoint:
    health:
      show-details: always

prometheus:
  metrics:
    export:
      enabled: true

merchantDetails: http://203.192.228.125:1002/view-kyc/app/public/fetchById?mid=
kyc.recent.minutes: 60 
 
contextPath: http://203.192.228.125:1002/kyc-report